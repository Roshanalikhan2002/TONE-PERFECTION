<section style="max-width:800px;margin:0 auto;padding:50px 36px 80px;">
  <h1 class="faq-title">{{ article.title }}</h1>
  <p class="signup-note">{{ article.published_at | date: '%B %d, %Y' }} &middot; {{ article.author }}</p>
  <div class="bundle-text">{{ article.content }}</div>
</section>
